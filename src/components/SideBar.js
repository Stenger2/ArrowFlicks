import React from "react";
import {Box, Drawer, Divider, IconButton, List, ListItem, ListItemButton , ListItemIcon, ListItemText} from  "@mui/material"
import Logo from "../img/logo.png"

export default function SideBar() {
  // const location = useLocation();
  // const navigate = useNavigate()
  return (
    <div>
      <span className="logo">ArrowFlicks</span>
    </div>
  );
}
